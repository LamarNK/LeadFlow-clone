[modules]
autoload=yes

; The legacy chan_sip module registers the same WebSocket sub-protocol "sip"
; before PJSIP and prevents res_pjsip_transport_websocket from loading.
; Orbita uses PJSIP exclusively for both the provider trunk and browser clients.
noload=chan_sip.so
