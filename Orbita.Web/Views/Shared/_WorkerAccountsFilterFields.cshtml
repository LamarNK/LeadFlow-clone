@model WorkerDetailsViewModel
<div class="orbita-filters-fields worker-accounts-filters-row" id="workerAccountsFilterPanel" data-orbita-collapsible-filters hidden>
    <label class="orbita-filters-field worker-accounts-filter-field">
        <span class="orbita-filters-label worker-accounts-filter-label">Источник</span>
        <select name="provider" class="orbita-filters-select" aria-label="Источник профилей">
            @foreach (var opt in Model.AccountProviderOptions)
            {
                <option value="@opt.Value" selected="@(opt.Value == (Model.AccountProvider ?? ""))">@opt.Label</option>
            }
        </select>
    </label>
    <label class="orbita-filters-field worker-accounts-filter-field">
        <span class="orbita-filters-label worker-accounts-filter-label">Группа / папка</span>
        <select name="groupId" class="orbita-filters-select" aria-label="Группа AdsPower или папка Multilogin">
            @foreach (var opt in Model.AccountGroupOptions)
            {
                <option value="@opt.Value" selected="@(opt.Value == (Model.AccountGroupId ?? ""))">@opt.Label</option>
            }
        </select>
    </label>
</div>
