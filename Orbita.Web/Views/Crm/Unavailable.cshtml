@{
    ViewData["Title"] = "CRM недоступна";
    var message = ViewData["CrmUnavailableMessage"] as string
        ?? "Не удалось загрузить CRM для выбранного офиса.";
    var header = new PageHeaderViewModel
    {
        Title = "CRM недоступна",
        Subtitle = message
    };
}
<partial name="_PageHeader" model="header" />
